{
  "name": "prawie_twitter",
  "version": "1.0.0",
  "description": "A basic twitter alternative",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": " npm ci --include=dev && cd server && npm ci --include=dev && cd ../client && npm ci --include=dev",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "grupa 2.",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
